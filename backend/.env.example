TZ=UTC
PORT=3333

# IMPORTANT: Use 0.0.0.0 to bind to all interfaces (IPv4 + IPv6)
# Using 'localhost' may only bind to IPv6, causing frontend proxy issues
HOST=0.0.0.0

LOG_LEVEL=info
APP_KEY=CHANGE_ME_TO_RANDOM_STRING
NODE_ENV=development

# Database Configuration (PostgreSQL)
# Make sure Docker Compose is running: docker-compose up -d
DB_CONNECTION=postgres
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_DATABASE=karsis_b2b_eshop

SESSION_DRIVER=cookie
